# Python 字节码 / 日志 / 本地导出压缩包
__pycache__/
*.pyc
*.log
*.zip
server.log
fund_arb_service.log

# 本地临时文件（部署后会由启动校准重新生成）
# weights_cache.json / holdings_mode_cache.json 已作为种子提交，可保留

# WorkBuddy 内部目录（记忆/agent 状态，不应进仓库）
.workbuddy/

# 本地调试/探针脚本与临时导出（不进仓库）
_probe_sources.py
bench_error.py
_top_api.json
d.json
top_check.json
rank_test.json

# 推送锁状态文件（运行时生成，含当日推送占用标记，不应进仓库）
.push_lock.json
.push_lock.json.tmp
